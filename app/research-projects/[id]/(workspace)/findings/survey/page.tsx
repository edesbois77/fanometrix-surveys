// Findings · Survey — the survey findings, referencing question-level valid
// response counts.
import { SourceFindingsSection } from "@/app/components/research-projects/findings/SourceFindingsSection";

export default function SurveyFindingsPage() {
  return <SourceFindingsSection kinds={["survey"]} />;
}
