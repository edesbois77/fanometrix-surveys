// Survey Studio Home — the first real page inside the new product shell.
// Rendered within app/survey-studio/layout.tsx (GlobalShell → StudioSidebar →
// workspace). All data comes from existing read-only APIs; no DB/API change.
import { StudioHome } from "@/app/components/studio/home/StudioHome";

export default function SurveyStudioHomePage() {
  return <StudioHome />;
}
