// Discover → Dashboard (index). The useful landing across all research the caller
// can access, from the governed context endpoint (entitlement unchanged).
import { DiscoverDashboard } from "@/app/components/studio/discover/DiscoverDashboard";

export default function DiscoverDashboardPage() {
  return <DiscoverDashboard />;
}
