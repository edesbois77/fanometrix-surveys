// Product Walkthrough's view of Key Findings — a thin re-export of the
// /research-projects report page, which derives its back-link from the
// current path so navigation stays inside the /product-walkthrough tree.
// See the survey stub for the full rationale.
export { default } from "@/app/research-projects/[id]/reports/key-findings/page";
