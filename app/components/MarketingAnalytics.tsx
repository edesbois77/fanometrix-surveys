"use client";

// TEMPORARILY DISABLED — analytics are off pending the first-party consent gate.
// Google Analytics + Microsoft Clarity will be re-enabled through ConsentBanner
// on a deny-by-default basis (they only load after the visitor accepts).
export function MarketingAnalytics() {
  return null;
}
